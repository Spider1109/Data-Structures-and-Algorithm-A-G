package arrays1d;

public class BarrCHart {
	public static void barrChart(int [] arr) {
		int n= arr.length;
		int max = Integer.MIN_VALUE;
		for(int i=0; i<n; i++) {
			max = Math.max(max, arr[i]);
		}
		for(int i=0; i<=max; i++) {
			int digit = arr[i];
			for(int j=0; j<)
		}
	}
}
